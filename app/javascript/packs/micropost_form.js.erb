$('#micropost_image').bind('change', function() {
    var size_in_megabytes = this.files[0].size/1024/1024;
    var max_size = <%= Settings.image.max %>
    if (size_in_megabytes < max_size) {
      alert(I18n.t("micropost.max_size"));
    }

  });
